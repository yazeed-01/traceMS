# TraceMS pipeline config: define your microservices and their order.
# Copy to pipeline.yaml or set PIPELINE_CONFIG_PATH to this file.
# Gateway will call services in order; each receives payload and returns updated payload + steps.

services:
  - name: validator
    url: http://validator:8001
    icon: "✓"
    description: Validate input
  - name: transformer
    url: http://transformer:8002
    icon: "⇅"
    description: Transform content
  - name: enricher
    url: http://enricher:8003
    icon: "⊕"
    description: Add metadata
  - name: persister
    url: http://persister:8004
    icon: "💾"
    description: Persist result

# Supported payload types: text, json, image, video, binary
# Payload format: { "type": "<type>", "data": "<string or base64>", "metadata": {} }
